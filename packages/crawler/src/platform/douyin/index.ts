/**
 * douyin 平台客户端门面 —— 聚合该平台抓取能力(a_bogus 签名 client + UA/ttwid 常量)。
 * import 统一从 `../../platform/douyin` 进,不直连内部文件。
 */
export * from "./abogus.ts"
export * from "./client.ts"
export * from "./stream.ts"
