{
  "name": "@tauri-playground/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri:dev": "cd ../src-tauri && tauri dev",
    "tauri:build": "cd ../src-tauri && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "vite": "^8.1.5",
    "typescript": "~7.0.2"
  }
}
